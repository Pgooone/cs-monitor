# Progress Log

# CS2 饰品价格波动监控系统 - Agent 开发进度

## 2026-04-14 - Task 1: 项目脚手架与基础配置

### What was done:
- 确认 requirements.txt、.env.example、config.py、README.md 已存在
- 创建 utils/logger.py，配置 loguru 日志输出（含控制台彩色日志和文件日志）
- 确认目录结构完整（api/, core/, notify/, storage/, utils/, data/, tests/）
- 运行 init.sh 初始化虚拟环境和依赖
- 语法检查通过

### Testing:
- python -m py_compile utils/logger.py config.py main.py 无错误
- .venv 环境创建成功，依赖安装完成

### Notes:
- .env 已配置 STEAMDT_API_KEY
- Git 仓库已初始化并提交初始 commit

## 2026-04-14 - Task 2: SteamDT API 封装模块

### What was done:
- 创建 api/steamdt.py，实现 SteamDTClient 类
- 实现 get_items_batch() 批量查询饰品价格（支持 1-100 个）
- 实现 get_7day_average() 查询 7 天均价
- 实现 get_all_items() 获取饰品基础信息
- 实现 get_item_kline() 查询 K 线数据（预留接口）
- 统一错误处理：重试 3 次、间隔 10 秒、1-3 秒随机延迟防风控
- 认证方式：Authorization: Bearer API_KEY
- Base URL 使用 https://open.steamdt.com

## 2026-04-14 - Task 3: 数据存储模块（SQLite）

### What was done:
- 创建 storage/models.py，定义 6 张表的建表 SQL
- 创建 storage/database.py，封装 Database 类
- 实现自动建表（首次运行时 _init_tables）
- 实现 items、price_records、alert_logs、extreme_track_snapshots、extreme_track_alerts 的 CRUD 方法
- 包括：insert_item, get_item, insert_price_record, get_latest_price, insert_alert_log, get_recent_alerts
- 包括：insert_extreme_snapshot, get_latest_snapshot, insert_extreme_alert, get_latest_extreme_alert

### Testing:
- python -m py_compile storage/database.py storage/models.py 无错误
- tests/test_storage.py 6 个测试全部通过
## 2026-04-15 - Task 4: 核心监控逻辑：价格采集与调度

### What was done:
- 创建 core/monitor.py，实现 PriceMonitor 类
- 实现 collect_prices()：读取 config.watchlist，批量调用 SteamDT API 获取当前价格
- 解析 API 响应并写入 price_records 表，同时处理 items 表外键约束
- 创建 core/scheduler.py，实现 MonitorScheduler 类
- 使用 APScheduler BackgroundScheduler 注册普通监控定时任务
- 配置默认每 30 分钟执行一次价格采集
- 首次启动时立即执行一次价格采集
- 支持优雅关闭调度器

### Testing:
- python -m py_compile core/monitor.py core/scheduler.py tests/test_monitor.py 无错误
- tests/test_monitor.py 4 个测试全部通过
- 全部 16 个单元测试通过
- python main.py 可正常启动

## 2026-04-15 - Task 5: 核心监控逻辑：波动分析与告警检测

### What was done:
- 创建 core/analyzer.py，实现 PriceAnalyzer 类
- 实现波动率计算：(当前价 - 基准价) / 基准价 × 100%
- 基准价优先使用 7 天均价（get_7day_average），缺失时回退到上一次采集价格
- 实现告警判断：涨幅 ≥ 阈值% 触发 price_surge，跌幅 ≤ -阈值% 触发 price_drop
- 实现告警冷却：同一饰品同一方向 4 小时内只告警 1 次
- 将触发的告警写入 alert_logs 表
- 集成 analyzer 到 monitor.py，每次采集后自动执行分析
- 为 database.py 添加 get_latest_price_any_platform() 支持无平台限制的基准价查询

## 2026-04-15 - Task 6: 极致追踪模块（高频单品狙击）

### What was done:
- 创建 core/extreme_tracker.py，实现 ExtremeTracker 类
- 使用统一 tick 机制（每 10 秒）管理多个追踪项，各追踪项按自定义间隔独立执行
- 支持 any 模式（任何变动通知）和 percent 模式（超百分比通知）
- 自动降频保护：API 返回 429 时自动翻倍间隔（最大 1 小时），连续成功 10 次后逐步恢复
- 价格和数量同时变动时合并为 both 类型告警写入 extreme_track_alerts
- 支持 alert_cooldown_seconds 冷却期和 quiet_hours 免打扰时段
- 集成到 scheduler.py，注册独立的 extreme_tracker 定时任务
- database.py 新增 get_latest_price_any_platform() 和 get_recent_extreme_alerts() 支持新功能

### Testing:
- python -m py_compile core/extreme_tracker.py core/scheduler.py tests/test_extreme_tracker.py 无错误
- tests/test_extreme_tracker.py 7 个测试全部通过（首次运行、价格告警、合并告警、冷却、429降频、禁用、免打扰）
- 全部 28 个单元测试通过

### Notes:
- PriceMonitor 对 API 失败、空 watchlist、异常响应都做了防御性处理
- 首次采集在 scheduler.start() 中立即触发
- analyzer 取各平台最低价为当前价进行波动判断

### Testing:
- python -m py_compile api/steamdt.py 无错误
- tests/test_api.py 6 个测试全部通过

## 2026-04-15 - Task 7: 通知推送模块

### What was done:
- 创建 notify/base.py，定义 NotificationChannel 抽象基类
- 创建 notify/wecom.py，实现企业微信机器人 Webhook 推送（P0）
- 创建 notify/telegram.py，实现 Telegram Bot 推送（P1）
- 创建 notify/serverchan.py，实现 Server 酱推送（P1）
- 创建 notify/manager.py，实现 NotificationManager 负责消息格式化和渠道路由
- 支持通过 config.notify_channel 切换默认渠道
- 通知发送失败时重试 3 次（send_with_retry）
- 集成通知到 analyzer.py，普通监控触发告警后自动推送
- 集成通知到 extreme_tracker.py，极致追踪触发告警后自动推送
- 按照 PRD 模板格式化普通监控和极致追踪三种消息内容

### Testing:
- python -m py_compile notify/*.py tests/test_notify.py 无错误
- tests/test_notify.py 8 个测试全部通过（企微成功/失败、TG、Server酱、渠道创建、消息格式化）
- 全部 36 个单元测试通过

### Notes:
- 通知渠道配置依赖 .env 中的 Webhook URL / Bot Token
- 未配置时 channel 为 None，告警仅记录数据库不发送通知

## 2026-04-15 - Task 8: 主程序入口与优雅退出

### What was done:
- 重写 main.py 作为完整主程序入口
- 加载配置（MonitorConfig.from_env()），校验 STEAMDT_API_KEY 是否存在
- 初始化 SQLite 数据库（data/prices.db）
- 初始化 SteamDTClient 并传入配置
- 启动 MonitorScheduler 调度器，注册普通监控和极致追踪任务
- 首次启动立即执行一次价格采集（已在 scheduler.start() 中实现）
- 实现 Ctrl+C (SIGINT) 和 SIGTERM 优雅退出处理，关闭调度器和 HTTP 客户端
- 添加主循环保持程序常驻运行
- 修正 .env 中 STEAMDT_BASE_URL 为 https://open.steamdt.com

### Testing:
- python -m py_compile main.py 无错误
- python main.py 可正常启动、初始化、执行首次价格采集和波动分析
- 企微通知失败符合预期（因为 Webhook URL 为占位符）
- 全部 36 个单元测试仍然通过

### Notes:
- 主程序运行后会一直阻塞在 while True 循环中，直到收到中断信号
- 首次启动时会立即调用 collect_prices() 进行一轮采集

## 2026-04-15 - Task 9: 单元测试

### What was done:
- tests/test_api.py：6 个测试覆盖 SteamDTClient 批量查询、7天均价、基础信息、重试失败
- tests/test_analyzer.py：5 个测试覆盖波动检测、阈值判断、告警冷却、基准价回退
- tests/test_notify.py：8 个测试覆盖企微/TG/Server酱发送、消息格式化、渠道路由
- tests/test_monitor.py：4 个测试覆盖价格采集、空清单、API失败
- tests/test_extreme_tracker.py：7 个测试覆盖快照、价格/数量变动、合并告警、429降频、冷却、免打扰
- tests/test_storage.py：6 个测试覆盖数据库表、CRUD操作

### Testing:
- python -m pytest tests/ -v 全部 36 个测试通过
- 所有核心模块均有测试覆盖

### Notes:
- 所有外部 API 调用在单元测试中使用 unittest.mock 模拟
- 数据库测试使用 tempfile 创建临时 SQLite 文件

## 2026-04-15 - Task 10: 最终集成测试与优化

### What was done:
- 运行全部 36 个单元测试，全部通过
- 运行 ruff check . --fix，修复 2 个未使用 import 问题
- 运行 mypy，除 apscheduler 第三方库缺失类型存根外无其他错误
- 验证 main.py 可正常启动、初始化、执行首次价格采集和波动分析
- 验证普通监控模式完整流程：批量采集 → 写入数据库 → 波动分析 → 触发告警 → 通知推送
- 验证极致追踪模式已通过单元测试覆盖：高频轮询 → 快照对比 → 合并通知
- 验证 429 自动降频、告警冷却、免打扰时段逻辑全部通过测试
- 代码格式统一，清理未使用 import

### Testing:
- python -m pytest tests/ -v：36 passed
- python -m ruff check .：0 errors
- python -m mypy .：仅 apscheduler 缺少 stubs（第三方库问题）
- python main.py：启动正常，API 调用成功，首次采集完成

### Notes:
- 项目核心功能已全部开发完成
- 用户只需在 .env 中配置真实的 Webhook URL 即可接收通知
- 企微通知在测试中出现 93000 错误是因为使用了占位符 Webhook URL

---

## 2026-04-18 - Task 11: Web API 骨架（FastAPI）

### What was done:
- 安装 fastapi、uvicorn 并更新 requirements.txt
- 创建 web/app.py：FastAPI 应用入口，含 CORS 配置（允许 localhost:5173）
- 创建 web/schemas.py：定义 HealthResponse、DashboardSummary、AlertRecord、PriceRecord Pydantic 模型
- 创建 web/routers/dashboard.py：实现 /api/dashboard/summary 概览端点
- 修改 main.py：同时启动 APScheduler（后台线程）+ FastAPI/uvicorn（主线程 8080 端口）
- 修改 config.py：新增 web_host、web_port 配置，支持从环境变量读取
- 给 storage/database.py 新增统计查询方法：get_today_alert_count、get_latest_price_records_count、get_latest_price_record_any
- 创建 tests/test_web_api.py：3 个测试覆盖 health、dashboard summary、CORS preflight

### Testing:
- python -m py_compile main.py config.py web/*.py web/routers/*.py storage/database.py 无错误
- python -m pytest tests/ -v：39 个测试全部通过（原有 36 + 新增 3）
- python -m ruff check .：0 errors
- python main.py：正常启动，调度器 + Web 服务同时运行，首次价格采集成

### Notes:
- 依赖注入通过 request.app.state 传递 db 和 config，避免循环导入
- CLI 监控功能未被破坏，scheduler 仍在后台线程正常运行

---

## 2026-04-18 - PRD Update: v2.0 Web 仪表盘

### Update Description:
基于《CS2 饰品监控 Web 仪表盘 — 后续开发 PRD》更新 agent workspace，追加 Web 仪表盘功能开发任务。

### New Tasks Added (12):
- Task 11: Web API 骨架（FastAPI）
- Task 12: 数据库迁移：监控清单持久化
- Task 13: Watchlist API 与 Alerts API
- Task 14: 价格数据 API 与极致追踪 API
- Task 15: Vue 3 前端初始化与 Dashboard 首页
- Task 16: 监控清单页与饰品详情页
- Task 17: 极致追踪页、告警页与设置页
- Task 18: 前端打包与静态文件托管
- Task 19: K线图表、价差看板与 WebSocket 推送
- Task 20: 趋势分析与极致追踪实时面板
- Task 21: 用户认证与数据归档
- Task 22: Docker 容器化与性能优化

### Files Updated:
- task.json：追加 12 个新任务（id 11-22），passes 初始化为 false
- architecture.md：新增 Web 层、前端层、新数据模型、API 设计、Docker 方案
- CLAUDE.md：更新项目结构、命令列表、编码规范（增加前端规范）、阻塞场景
- init.sh：增加 Node.js 检查、前端 npm install
- .env.example：增加 WEB_HOST、WEB_PORT 配置
- README.md：增加 Web 仪表盘功能说明、开发模式、技术栈
- QUICKSTART.md：更新任务清单数量、常用命令
- progress.txt：追加 PRD 更新记录

### Architecture Changes:
- 新增 web/ 目录（FastAPI 应用、路由、Pydantic 模型、WebSocket）
- 新增 frontend/ 目录（Vue 3 + Vite + TypeScript + Naive UI）
- main.py 改造：同时启动 APScheduler + FastAPI uvicorn
- storage/models.py 新增 watchlist、extreme_track_config、system_config 表
- 采用 DB 优先策略：config.py 仅作初始默认值

## 2026-04-18 - Task 12: 数据库迁移：监控清单持久化

### What was done:
- storage/models.py 新增 3 张表：
  - watchlist 表（market_hash_name, display_name, threshold_percent, enabled, created_at, updated_at）
  - extreme_track_config 表（完整的追踪配置字段，含 interval_seconds、price/quantity 追踪模式、阈值、冷却、免打扰等）
  - system_config 表（key-value 系统配置）
- storage/database.py 新增 CRUD 方法：
  - watchlist: insert/get/update/delete + get_watchlist_threshold
  - extreme_track_config: insert/get/update/delete
  - system_config: set/get
  - import_default_watchlist() / import_default_extreme_track()：首次启动时从 config.py 默认值自动导入
  - 数据库连接启用 WAL 模式（PRAGMA journal_mode = WAL）
- core/monitor.py：collect_prices() 改为从 self.db.get_watchlist() 读取监控清单
- core/analyzer.py：_get_threshold() 改为从 DB 读取，不存在时回退到 config.default_threshold_percent
- core/extreme_tracker.py：tick() 改为从 self.db.get_extreme_track_configs() 读取追踪配置
- core/scheduler.py：_add_extreme_tracker_job() 改为从 DB 检查列表是否为空
- main.py：初始化数据库后调用默认数据导入方法
- 保持 config.py 作为初始默认值兜底，DB 优先策略

### Testing:
- python -m py_compile 所有修改文件无语法错误
- python -m ruff check .：0 errors
- python -m pytest tests/ -v：39 个测试全部通过
- 修正 test_monitor.py 和 test_extreme_tracker.py 的 fixture：测试数据通过 DB 插入而非仅配置 config

### Notes:
- DB 优先策略已落地：运行时所有清单数据从 SQLite 读取，config.py 仅在首次启动时作为默认种子
- 新增表结构兼容后续 Web API（Task 13/14）的 CRUD 操作
- WAL 模式启用，支持并发读写，避免 Web API 和 scheduler 之间的数据库锁冲突

## 2026-04-18 - Task 13: Watchlist API 与 Alerts API

### What was done:
- 扩展 web/schemas.py：新增 WatchlistItem、WatchlistItemCreate、WatchlistItemUpdate、WatchlistItemWithPrice、AlertRecordFilter、AlertStatsItem、AlertStatsResponse 等 Pydantic 模型
- 创建 web/routers/watchlist.py：实现监控清单完整 CRUD
  - GET /api/watchlist：获取全部监控清单（含最新价格，通过 LEFT JOIN 子查询）
  - POST /api/watchlist：添加监控项（重复返回 409）
  - PUT /api/watchlist/{market_hash_name}：更新监控项（不存在返回 404）
  - DELETE /api/watchlist/{market_hash_name}：删除监控项
- 创建 web/routers/alerts.py：实现告警记录查询和统计
  - GET /api/alerts：分页查询告警（支持 page/limit、alert_type 过滤、start_date/end_date 时间范围、market_hash_name 模糊搜索）
  - GET /api/alerts/stats：按天/类型聚合统计，返回 total、by_day、by_type
- storage/database.py 新增查询方法：get_alerts()、get_alert_stats()、get_watchlist_count()、get_extreme_track_count()、get_watchlist_with_latest_price()
- 更新 web/routers/dashboard.py：active_watchlist 和 extreme_track_count 改为从 DB 读取（DB 优先策略）
- 更新 web/app.py：注册 watchlist 和 alerts 路由
- tests/test_web_api.py 新增 12 个测试覆盖 watchlist CRUD 和 alerts 查询/统计

### Testing:
- python -m py_compile 所有修改文件无语法错误
- python -m pytest tests/ -v：51 个测试全部通过（原有 39 + 新增 12）
- python -m ruff check .：0 errors
- python main.py：可正常导入和启动

### Notes:
- DashboardSummary 中 active_watchlist 和 extreme_track_count 已完全从 DB 读取，不再依赖 config.py
- get_watchlist_with_latest_price 使用关联子查询获取每个饰品最新的价格记录
- alerts 查询支持完整的时间范围过滤和分页

## 2026-04-18 - Task 14: 价格数据 API 与极致追踪 API

### What was done:
- 扩展 web/schemas.py：新增 LatestPriceItem、PriceHistoryItem、PlatformPriceItem、ExtremeTrackConfig、ExtremeTrackConfigCreate、ExtremeTrackConfigUpdate Pydantic 模型
- 创建 web/routers/prices.py：实现价格数据查询接口
  - GET /api/prices/latest：获取所有监控品最新价格（每饰品每平台各取最新）
  - GET /api/prices/{name}/history：获取历史价格，支持 days 和 platform 过滤
  - GET /api/prices/{name}/platforms：获取指定饰品在各平台的最新价格
- 创建 web/routers/extreme_track.py：实现极致追踪完整 CRUD + toggle 启停
  - GET /api/extreme-track：获取全部追踪配置
  - POST /api/extreme-track：添加追踪配置（重复返回 409）
  - PUT /api/extreme-track/{name}/{platform}：更新追踪配置（不存在返回 404）
  - DELETE /api/extreme-track/{name}/{platform}：删除追踪配置
  - POST /api/extreme-track/{name}/{platform}/toggle：切换启停状态
- storage/database.py 新增查询方法：get_latest_prices()、get_price_history()、get_price_by_platforms()
- 更新 web/app.py：注册 prices 和 extreme_track 路由
- tests/test_web_api.py 新增 12 个测试覆盖 prices 查询和 extreme_track CRUD/toggle

### Testing:
- python -m py_compile 所有修改文件无语法错误
- python -m pytest tests/ -v：65 个测试全部通过（原有 51 + 新增 14）
- python -m ruff check .：0 errors
- python main.py：可正常导入和启动

### Notes:
- prices 路由中的 {market_hash_name} 支持 URL 编码的特殊字符（如空格、|）
- get_price_history 的 days 参数使用 SQLite datetime 字符串拼接（参数绑定不支持间隔值）
- extreme_track 的 toggle 端点返回布尔值 enabled，便于前端直接更新状态

## 2026-04-18 - Task 15: Vue 3 前端初始化与 Dashboard 首页

### What was done:
- 创建 frontend/package.json（Vue 3 + Vite + TypeScript + UnoCSS）
- 创建 frontend/vite.config.ts，配置 /api 代理到 localhost:8080
- 创建 tsconfig.json / tsconfig.node.json 严格模式配置
- 创建 frontend/src/main.ts，集成 Vue Router + Pinia + Naive UI
- 创建基础布局 AppLayout.vue：可折叠侧边栏导航 + 主内容区
- 创建 axios 封装 frontend/src/api/index.ts，定义 DashboardSummary / WatchlistItemWithPrice / AlertRecord 类型
- 创建 Pinia store dashboard.ts，管理 Dashboard 数据状态
- 实现 Dashboard.vue 首页：4 个统计卡片 + 监控价格概览表 + 最近告警列表
- 创建 Watchlist.vue / ExtremeTrack.vue / Alerts.vue / Settings.vue 占位页面（支持路由切换）
- 路由配置：/ → Dashboard, /watchlist, /extreme-track, /alerts, /settings

### Testing:
- cd frontend && npm install：依赖安装成功
- cd frontend && npm run build：vue-tsc + vite build 通过，无编译错误
- python -m pytest tests/ -v：65 个后端测试全部通过，无回归
- python -m ruff check .：0 errors

### Notes:
- 前端路由使用懒加载（Dashboard 除外），优化首屏加载
- 统计卡片使用 naive-ui NStatistic + NCard 组件
- 表格使用 NDataTable，支持分页和溢出提示
- 构建产物输出到 frontend/dist（为 Task 18 静态文件托管做准备）

## 2026-04-18 - Task 16: 监控清单页与饰品详情页

### What was done:
- 扩展 frontend/src/api/index.ts：
  - 新增 WatchlistItem、PriceHistoryItem、PlatformPriceItem、CreateWatchlistPayload、UpdateWatchlistPayload 类型
  - 新增 createWatchlistItem、updateWatchlistItem、deleteWatchlistItem、priceHistory、platformPrices API 方法
- 创建 Pinia store stores/watchlist.ts：
  - 管理监控清单状态（items、loading、error）
  - 实现 fetchItems、addItem、updateItem、removeItem、toggleEnabled 方法
- 实现 Watchlist.vue 完整功能：
  - 表格展示监控清单（名称、当前价格、平台、阈值、状态、操作）
  - 点击饰品名称跳转 ItemDetail 详情页
  - 添加/编辑弹窗（表单验证、名称不可重复）
  - 启停开关（表格内切换启用/禁用状态）
  - 删除确认（对话框二次确认）
- 实现 ItemDetail.vue 饰品详情页：
  - 顶部 3 个统计卡片（当前价格、均价、波动率）
  - 各平台价格对比卡片（grid 布局）
  - ECharts 价格走势折线图（支持 7天/30天切换，面积图）
  - 该饰品的告警历史表格（类型、当前价、基准价、波动、时间）
- 更新 src/router/index.ts：新增 /item/:name 路由
- 更新 AppLayout.vue 页面标题映射：新增 ItemDetail → 饰品详情

### Testing:
- cd frontend && npm run build：vue-tsc + vite build 通过，无编译错误
- python -m pytest tests/ -v：65 个后端测试全部通过，无回归
- python -m ruff check .：0 errors

### Notes:
- ECharts 按需全量引入（当前仅折线图），后续 K 线图（Task 19）可复用图表初始化逻辑
- 详情页通过 route.params.name 获取饰品名称，使用 encodeURIComponent/解码处理特殊字符
- 价格走势图按 recorded_at 升序排列，x 轴显示月日格式
- 告警历史调用 /api/alerts?market_hash_name=xxx 接口过滤

---

## 2026-04-18 - Task 17: 极致追踪页、告警页与设置页

### What was done:
- 新建后端 web/routers/settings.py：
  - GET /api/settings/notify：读取通知配置（DB 优先，fallback 到 .env）
  - PUT /api/settings/notify：保存通知配置到 system_config 表
  - POST /api/settings/notify/test：发送测试通知（当前支持企微）
- 扩展 web/schemas.py：新增 NotifySettings、NotifyTestRequest Pydantic 模型
- 更新 web/app.py：注册 settings 路由
- 扩展 frontend/src/api/index.ts：
  - 新增 ExtremeTrackConfig 相关类型和 API（extremeTrackList、createExtremeTrack、updateExtremeTrack、deleteExtremeTrack、toggleExtremeTrack）
  - 新增 alertStats、getNotifySettings、updateNotifySettings、testNotify API
- 创建 Pinia store stores/extremeTrack.ts：管理极致追踪状态（CRUD + toggle）
- 创建 Pinia store stores/alerts.ts：管理告警记录状态（分页查询 + 统计）
- 实现 ExtremeTrack.vue：
  - 表格展示追踪项（饰品、平台、间隔、价格/数量追踪模式、状态、操作）
  - 添加/编辑弹窗（含价格/数量追踪开关、模式选择、阈值、冷却、免打扰时段）
  - 启停开关、删除确认
- 实现 Alerts.vue：
  - 筛选区：告警类型、开始/结束日期、饰品名称搜索
  - ECharts 柱状图展示按天告警趋势
  - 分页告警列表（类型标签化、价格/波动格式化）
- 实现 Settings.vue：
  - 通知渠道选择（企微/Telegram/Server酱）
  - 各渠道对应配置表单输入
  - 保存配置按钮、测试通知按钮

### Testing:
- cd frontend && npm run build：vue-tsc + vite build 通过，无编译错误
- python -m py_compile web/routers/settings.py web/app.py web/schemas.py：无语法错误
- python -m pytest tests/ -v：65 个后端测试全部通过，无回归
- python -m ruff check .：0 errors

### Notes:
- 后端 settings 路由使用 system_config 表持久化通知配置，与 config.py 解耦
- 前端三个页面均使用 Naive UI 组件 + Pinia 状态管理，保持与 Watchlist.vue 一致的交互模式
- Alerts.vue 柱状图在筛选条件变化时自动重绘

---

## 2026-04-18 - Task 18: 前端打包与静态文件托管

### What was done:
- 运行 `cd frontend && npm run build` 生成生产构建产物到 frontend/dist
- 修改 web/app.py：
  - 引入 `FileResponse`、`HTTPException` 和 `Path`
  - 添加 catch-all 路由 `/{full_path:path}`，实现 SPA fallback
  - 对存在的静态文件（CSS/JS/图片）直接返回文件内容
  - 对不存在的路径回退到 index.html，由 Vue Router 处理前端路由
  - API 路径（/api/*）正常抛出 404，不干扰已有 API 路由
- 更新 README.md「快速开始」章节：
  - 步骤 3 增加 `npm run build` 构建说明
  - 明确说明构建后访问 `http://localhost:8080` 即可直接打开仪表盘
  - 步骤 4 开发模式增加提醒：开发完成后需重新构建使生产环境生效

### Testing:
- `cd frontend && npm run build`：vue-tsc + vite build 通过，无编译错误
- TestClient 测试：GET / 返回 index.html，GET /watchlist 返回 index.html（SPA fallback），GET /assets/... 返回静态文件，GET /api/health 返回 JSON，GET /docs 正常可用
- 实际启动服务测试：curl 验证 /、/watchlist、/assets/、/api/health 均正常
- `python -m pytest tests/ -v`：65 个测试全部通过，无回归
- `python -m ruff check .`：0 errors
- `python -m py_compile web/app.py main.py`：无语法错误

### Notes:
- SPA fallback 使用 catch-all route 而非 `StaticFiles(html=True)`，后者对非根路径不会自动回退到 index.html
- 前端构建产物（frontend/dist）不提交到 git，由 CI/CD 或部署时构建

---

## 2026-04-18 - Task 19: K线图表、价差看板与 WebSocket 推送

### What was done:
- 后端：创建 web/ws_manager.py，实现 WebSocketManager 全局单例
  - 支持 /ws/alerts 告警实时推送频道和 /ws/extreme-track/{name}/{platform} 极致追踪实时数据流频道
  - 提供 sync 版本广播方法（broadcast_alert_sync / broadcast_extreme_track_sync），支持从 APScheduler 后台线程安全地向 FastAPI 事件循环推送消息
- 后端：创建 web/routers/kline.py，实现 K 线和价差 API
  - GET /api/kline/{name}：查询饰品 K 线数据，支持 period（1=时K, 2=日K, 3=周K）和 count 参数，调用 SteamDTClient.get_item_kline()
  - GET /api/arbitrage：所有监控品跨平台价差列表，按价差百分比降序排列
  - GET /api/arbitrage/{name}：单品各平台价差明细
  - 为避免 /kline/{name} 与 /kline/arbitrage 路由冲突，将 arbitrage 路由独立为 arbitrage_router（prefix=/arbitrage）
- 后端：修改 web/app.py，注册 kline/arbitrage 路由，添加两个 WebSocket endpoint
- 后端：修改 core/monitor.py，collect_prices() 返回 {"records": ..., "alerts": ...}，使告警可被外部获取
- 后端：修改 core/scheduler.py，新增 _run_monitor() 和 _run_extreme_tracker() 包装方法，在任务执行后通过 ws_manager 广播实时消息
- 前端：扩展 frontend/src/api/index.ts，新增 kline、arbitrage、arbitrageItem API 方法和 KlineResponse / ArbitrageItem 类型
- 前端：创建 frontend/src/utils/ws.ts，实现 WebSocketClient 类（自动重连、指数退避、心跳 ping/pong）
- 前端：修改 ItemDetail.vue，集成 ECharts K 线图（OHLC + MA5/MA10/MA20 均线 + 成交量柱状图），支持价格走势/K线图切换，支持时K/日K/周K切换
- 前端：修改 Alerts.vue，集成 WebSocket 连接 /ws/alerts，收到实时告警后通过 store.prependAlert() 插入列表顶部
- 前端：修改 stores/alerts.ts，新增 prependAlert() 方法支持实时插入
- 前端：修改 ExtremeTrack.vue，为每个启用的追踪项建立 WebSocket 连接（连接池管理），收到实时数据后更新到 store
- 前端：修改 stores/extremeTrack.ts，新增 updateRealtimeData() 方法存储实时数据

### Testing:
- cd frontend && npm run build：vue-tsc + vite build 通过，无编译错误
- python -m py_compile 所有修改文件无语法错误
- python -m pytest tests/ -v：65 个测试全部通过（修复 test_monitor.py 适配新的 collect_prices 返回格式）
- python -m ruff check .：0 errors
- FastAPI 应用初始化测试通过

### Notes:
- WebSocket 的 sync 广播通过 asyncio.run_coroutine_threadsafe() 实现，首次 WS 连接时自动捕获事件循环
- K 线图红绿配色采用国内习惯（涨红跌绿）
- 前端 WS 客户端支持指数退避重连（初始 3 秒，最大 30 秒）

---

## 2026-04-18 - Task 20: 趋势分析与极致追踪实时面板

### What was done:
- 后端：创建 core/trend_analyzer.py，实现 TrendAnalyzer 类
  - 从 DB 按天聚合价格（get_daily_prices），计算 MA5/MA10/MA20 移动平均线
  - 趋势标签检测：最近连续 3 天上涨 → surge（连涨），连续 3 天下跌 → drop（连跌），否则 → oscillate（震荡）
  - 数据不足 5 天时返回 None
- 后端：扩展 storage/database.py，新增 get_daily_prices() 按天均价聚合查询方法
- 后端：扩展 web/routers/kline.py，新增 trends_router（prefix=/trends）
  - GET /api/trends/{name}：返回趋势标签、日价格序列、MA5/MA10/MA20 数据
- 后端：修改 web/app.py，注册 trends_router
- 后端：扩展 web/schemas.py，新增 DailyPricePoint、TrendAnalysisResponse Pydantic 模型
- 前端：扩展 frontend/src/api/index.ts，新增 trends() API 和 DailyPricePoint / TrendAnalysisResponse 类型
- 前端：修改 Dashboard.vue，价格概览表新增「趋势」列
  - 异步加载每个监控项的趋势标签（连涨/连跌/震荡）
  - 使用 NTag 展示，颜色区分（连涨红色、连跌绿色、震荡橙色）
- 前端：修改 ItemDetail.vue，新增趋势标签区域
  - 显示趋势标签（大尺寸 NTag）
  - 显示 MA5/MA10/MA20 最新值（小尺寸 NTag）
  - 趋势数据随页面加载一并请求
- 前端：修改 ExtremeTrack.vue，新增实时面板展开行
  - 使用 naive-ui DataTable 的 expand 列，点击展开查看实时变动
  - 创建组件 ExtremeTrackTimeline.vue，时间线式呈现价格/数量变化
  - 显示变动类型标签、时间、价格变化（含金额和百分比）、数量变化
- 前端：修改 stores/extremeTrack.ts，updateRealtimeData() 将 WS 实时数据附加到 item._realtime 数组

### Testing:
- cd frontend && npm run build：vue-tsc + vite build 通过，无编译错误
- python -m py_compile 所有修改文件无语法错误
- python -m pytest tests/ -v：75 个测试全部通过（原有 65 + 新增 10 个趋势分析测试）
- python -m ruff check .：0 errors
- python main.py：可正常导入和启动

### Notes:
- 趋势分析使用按天均价聚合，避免同一天多次采集导致数据噪音
- 实时面板仅展示最近 10 条变动记录，防止数据过多
- WebSocket 数据在 ExtremeTrackTimeline 中通过 timeline 组件直观展示变动方向（价格涨红跌绿、数量涨绿跌红）

---

## 2026-04-18 - Task 21: 用户认证与数据归档

### What was done:
- **JWT 认证中间件（单用户模式）**：
  - 新增 `web/deps.py`：实现 `require_auth` 依赖（HTTPBearer + PyJWT 验证），`create_access_token` 令牌生成
  - 新增 `web/routers/auth.py`：实现 `POST /api/auth/login` 登录端点（验证 admin_password，返回 JWT）
  - 扩展 `config.py`：新增 `admin_password`、`jwt_secret`、`jwt_expiry_hours` 配置，支持从环境变量读取
  - 更新 `.env.example`：新增 `ADMIN_PASSWORD`、`JWT_SECRET`、`JWT_EXPIRY_HOURS` 配置项
  - 更新 `requirements.txt`：添加 `pyjwt>=2.8.0`
- **API 路由登录保护**：
  - 所有 API 路由（dashboard、watchlist、alerts、prices、extreme-track、settings、kline、arbitrage、trends、archive）添加 `Depends(require_auth)`
  - `/api/health` 健康检查端点保持开放（无需认证，供 Docker/运维监控使用）
  - 重构所有 router：将 `get_db`/`get_config` 提取到 `web/deps.py`，统一依赖注入入口
- **数据归档逻辑**：
  - `storage/models.py`：新增 `archived_prices` 表（按天聚合归档：avg_price、min_price、max_price、record_count）
  - `storage/database.py`：新增 `archive_old_price_records()` 方法（聚合 90 天以上数据 → 写入归档表 → 删除原始记录），新增 `get_archived_price_history()` 查询方法
  - `core/scheduler.py`：新增每日凌晨 3 点 CronTrigger 归档任务 `_add_archive_job()`
  - 新增 `web/routers/archive.py`：`POST /api/archive/run` 手动触发归档接口
- **完善健康检查端点**：
  - `/api/health` 返回 `{"status", "version", "database", "scheduler"}`，包含数据库连接状态检查

### Testing:
- `python -m py_compile` 所有修改文件无语法错误
- `python -m pytest tests/ -v`：81 个测试全部通过（原有 75 + 新增 4 个认证测试 + 2 个归档测试）
- `python -m ruff check .`：0 errors
- `python main.py`：可正常启动，调度器包含归档任务
- 使用 curl 测试：未携带 Token 访问 /api/watchlist 返回 401，携带正确 Token 返回 200
- 手动触发归档 API 测试通过

### Notes:
- 单用户模式：无需注册，首次部署时通过 .env 的 ADMIN_PASSWORD 设置密码
- JWT 密钥长度低于 32 字节时 PyJWT 会发出 InsecureKeyLengthWarning（生产环境应设置更长的 JWT_SECRET）
- 归档任务每日凌晨 3:00 自动执行，不会影响实时价格查询
- 所有原有 API 测试已适配 JWT 认证（fixture 自动登录获取 Token）

---

### Development Guide:
按照 task.json 中 id 顺序，从 Task 11 开始依次完成。每个 Task 30-60 分钟，完成后提交 git commit。
